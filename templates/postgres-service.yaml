apiVersion: {{ .Values.service.apiversion }}
kind: Service
metadata:
  labels:
    io.fynarfin.service: postgres
  name: postgres
  namespace: odk  
spec:
  ports:
    - name: "5432"
      port: 5432
      targetPort: 5432
  selector:
    io.fynarfin.service: postgres
status:
  loadBalancer: {}
